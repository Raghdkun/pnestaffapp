# Compile-time secrets read by lib/core/config/env.dart (envied).
# These are non-secret placeholders so the project builds out of the box.
# Provide real values here locally or via CI; do NOT commit real secrets.
SENTRY_DSN=
API_KEY=
